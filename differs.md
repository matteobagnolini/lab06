URL https://github.com/matteobagnolini/lab06.git

CLONE git@github.com:matteobagnolini/lab06.git

BRANCH exercises

LAB 61-git-remotes-merge-conflict

LAB 62-use-lists-and-maps

LAB 63-exceptions

CASA 64-design-with-maps
L'idea alla base è la stessa, cioè utilizzare una Map, tuttavia variano le implementazioni dei setter e getter, che, nel controllare se un gruppo/utente esiste già, sono meno efficienti rispetto alla soluzione

CASA 65-implement-generic-class
Ho seguito lo strategy pattern creando un'interfaccia con metodo searchingAlgorithm e creato due classi BFS e DFS. Tuttavia non mi sono appoggiato a un metodo graphSearch, ma l'algoritmo è implementato esclusivamente nella classe apposita. Rispetto alla soluzione mancano anche le implementazioni di metodi di utility in GraphImpl (usati per fare dei check), che ho implementato direttamente nei metodi dove vengono usati. 